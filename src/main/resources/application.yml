spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rest-springboot-kotlin?useTimezone=true&serverTimezone=UTC
    username: root
    password: infor
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false